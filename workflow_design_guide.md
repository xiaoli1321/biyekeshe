# AI 多步文档工作流：从设计到全链路打字机效果实现

本指南详细记录了如何设计并实现一个基于 AI 的多步结构化文档生成系统。无论你是初学者还是有经验的开发者，这份“避坑指南”都将帮助你构建一个流畅、稳定且具备“打字机”实时输出效果的 AI 工作流。

---

## 1. 核心设计思想：什么是“AI 工作流”？

传统的 AI 对话是“一问一答”，而**工作流（Workflow）**是将复杂的任务分解为多个有序步骤。例如生成一份“分析报告”：
1. **步骤一 (Fixed)**：固定输出标题 `# 分析报告`。
2. **步骤二 (Text)**：调用 AI 输入主题，生成“执行摘要”。
3. **步骤三 (Text)**：调用 AI 基于前一步内容，进行“详细分析”。
4. **步骤四 (Text)**：调用 AI 总结“结论与建议”。

### 为什么选择线性流？
对于 Word 文档生成，**线性顺序流**是最直观且高效的，类似于人类写文章的逻辑。

---

## 2. 技术架构选型

| 模块 | 技术栈 | 选型原因 |
| :--- | :--- | :--- |
| **前端** | Vue 3 + Pinia | 响应式状态管理（Pinia）让多步骤内容更新逻辑非常清晰。 |
| **后端** | Java (Spring Boot) | 企业级稳定性，强类型检查减少 Bug；使用 WebClient 处理非阻塞流。 |
| **协议** | **SSE (Server-Sent Events)** | **最核心的选择**。AI 响应慢，用户不希望等一分钟直接看到 2000 字。SSE 允许服务器每秒“泵”出几个字，前端即时渲染。 |

---

## 3. 实现细节与避坑指南 (实战记录)

在这个功能的开发过程中，我们遇到了四个典型问题，以下是它们的“死法”与“生机”：

### 问题 A：SSE 内容迟延与“挤压感”
*   **现象**：AI 后台一直在吐字，但前端每隔 5-10 秒才蹦出一大坨字，没有打字机那种一个字一个字出来的感觉。
*   **原因**：后端没有及时 `flush()`。默认情况下，HTTP 响应会等待缓冲区满了才发出。
*   **解决方法**：
    *   后端每发送一个 `token` 事件后，立即调用 `writer.flush()`。
    *   统一 SSE 数据包格式：强制所有步骤（包含固定文本）都包装在 `data: {"type":"token", "content":"..."}` 中。

### 问题 B：网络分块导致的内容丢失 (粘包)
*   **现象**：`curl` 测试明明有内容，但在高并发或网络复杂时，AI 吐出的内容在后端解析时突然断掉，只显示标题。
*   **原因**：[WebClient](file:///e:/%E9%9D%A2%E8%AF%95%E5%87%86%E5%A4%87/%E6%AF%95%E4%B8%9A%E8%AE%BE%E8%AE%A1%E9%A1%B9%E7%9B%AE/learn__course/learn_coursev2/biyekeshe/src/main/java/com/learnplatform/service/workflow/LlmStreamingService.java#40-49) 以块（Chunk）形式接收数据。如果一个块里包含了两条 `data:` 线，原先按行读取的代码会因为 JSON 格式错乱而直接忽略掉后续内容。
*   **解决方法**：
    *   后端使用 Spring 标准的 `ParameterizedTypeReference<ServerSentEvent<String>>` 来消费 LLM 流。这保证了即使网络再怎么乱压包，底层框架也会帮你切成正确的“单条 SSE 事件”。

### 问题 C：Pinia 响应式“死锁”
*   **现象**：明明 API 有返回，浏览器控制台也报错了，但 UI 界面死活不动，一直显示“等待生成”。
*   **原因**：在 Vue 组件中直接解构了 Pinia Store 的 state（例如 `const { isRunning } = store`）。在 Vue 3 中，直接解构会**失去响应式**，变量值变了 UI 也不会重新渲染。
*   **解决方法**：
    *   使用 `const { isRunning } = storeToRefs(store)`。这保证了变量和 UI 是“量子纠缠”的，同步更新。

---

## 4. 为什么这么做？还有更好的吗？

### 1. 变量替换逻辑 `{{VAR_N}}`
我们在后端实现了将“第 N 步的结果”自动注入到“第 N+1 步 Prompt”的逻辑。
*   **现状**：简单的正则替换。
*   **更好方案**：引入 **LangChain4j** 等专业 AI 编排框架，可以实现更复杂的条件判断、重试机制和变量注入。

### 2. 本地 UUID 支持
*   **现状**：考虑到 Vite/ESM 模块在某些环境下对 [uuid](file:///e:/%E9%9D%A2%E8%AF%95%E5%87%86%E5%A4%87/%E6%AF%95%E4%B8%9A%E8%AE%BE%E8%AE%A1%E9%A1%B9%E7%9B%AE/learn__course/learn_coursev2/biyekeshe/learn-platform-frontend/src/views/WorkflowRun.vue#217-224) 库的解析问题，我们手动实现了一个轻量级 UUID 生成器。
*   **更好方案**：对于企业级应用，应在后端生成 Session ID 并存储在 Redis 中，防止刷新页面后状态丢失。

### 3. SSE 与 WebSockets
*   **SSE 优点**：单向流、自动重连、协议简单（基于 HTTP）。适合“AI 吐字”。
*   **WebSocket 优点**：双向通信。适合“实时多人在线协作”。
*   **结论**：本项目专注于文档生成，SSE 是性价比最高的选择。

---

## 5. 总结：小白如何开始设计？

1.  **定义协议**：先商量好后端吐出的 Token 长什么样（推荐统一 JSON 包装）。
2.  **后端主循环**：写一个 While 循环，一个个跑步骤，把每步的结果塞进“历史记录”里喂给下一步。
3.  **前端打字机**：前端维护一个数组 `stepMessages`，根据后端发来的 `token` 事件，实时 `+=` 对应的字符串。
4.  **容错处理**：一定要有“停止生成”按钮，调用后端的 Abort 接口。

> **提示**：永远记得在 SSE 发送后做一次 `flush()`，否则你会看到一个“便秘”的 AI。
